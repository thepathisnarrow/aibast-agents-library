# Token files (contain secrets)
.copilot_token
.copilot_session
.env

# Per-SE environment config (contains tenant/subscription IDs)
env.yaml

# Python
__pycache__/
*.pyc

# Remote agents (downloaded at runtime)
.remote_agents/
.repos.json
voice.zip
