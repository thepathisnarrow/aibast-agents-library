"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "makeStyles", {
    enumerable: true,
    get: function() {
        return makeStyles;
    }
});
const _index = require("./devtools/index.cjs");
const _insertionFactory = require("./insertionFactory.cjs");
const _resolveStyleRulesForSlots = require("./resolveStyleRulesForSlots.cjs");
const _reduceToClassNameForSlots = require("./runtime/reduceToClassNameForSlots.cjs");
function makeStyles(stylesBySlots, factory = _insertionFactory.insertionFactory) {
    const insertStyles = factory();
    let classesMapBySlot = null;
    let cssRules = null;
    let ltrClassNamesForSlots = null;
    let rtlClassNamesForSlots = null;
    let sourceURL;
    if (process.env.NODE_ENV !== 'production' && _index.isDevToolsEnabled) {
        sourceURL = (0, _index.getSourceURLfromError)();
    }
    let classNameHashSalt;
    function computeClasses(options) {
        const { dir, renderer } = options;
        if (classesMapBySlot === null) {
            [classesMapBySlot, cssRules] = (0, _resolveStyleRulesForSlots.resolveStyleRulesForSlots)(stylesBySlots, renderer.classNameHashSalt);
            if (process.env.NODE_ENV !== 'production') {
                if (renderer.classNameHashSalt) {
                    if (classNameHashSalt !== renderer.classNameHashSalt) {
                        console.error([
                            '@griffel/core:',
                            '\n\n',
                            'A provided renderer has different "classNameHashSalt".',
                            'This is not supported and WILL cause issues with classnames generation.',
                            'Ensure that all renderers created with "createDOMRenderer()" have the same "classNameHashSalt".'
                        ].join(' '));
                    }
                    classNameHashSalt = renderer.classNameHashSalt;
                }
            }
        }
        const isLTR = dir === 'ltr';
        if (isLTR) {
            if (ltrClassNamesForSlots === null) {
                ltrClassNamesForSlots = (0, _reduceToClassNameForSlots.reduceToClassNameForSlots)(classesMapBySlot, dir);
            }
        } else {
            if (rtlClassNamesForSlots === null) {
                rtlClassNamesForSlots = (0, _reduceToClassNameForSlots.reduceToClassNameForSlots)(classesMapBySlot, dir);
            }
        }
        insertStyles(renderer, cssRules);
        const classNamesForSlots = isLTR ? ltrClassNamesForSlots : rtlClassNamesForSlots;
        if (process.env.NODE_ENV !== 'production' && _index.isDevToolsEnabled) {
            _index.debugData.addSequenceDetails(classNamesForSlots, sourceURL);
        }
        return classNamesForSlots;
    }
    return computeClasses;
} //# sourceMappingURL=makeStyles.js.map
